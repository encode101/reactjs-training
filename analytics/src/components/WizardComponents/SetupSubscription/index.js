import React, { Component, PropTypes } from 'react';

class SetupSubscription extends Component {
    static propTypes = {
        // className: PropTypes.string,
    };

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <h5>Setup Subscription</h5>
        );
    }
}

export default SetupSubscription;
